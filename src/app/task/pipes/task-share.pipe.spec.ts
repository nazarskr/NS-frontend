import { TaskSharePipe } from './task-share.pipe';

describe('TaskSharePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskSharePipe();
    expect(pipe).toBeTruthy();
  });
});
